skip_tags: true
image: Visual Studio 2017
configuration: Release

install:
  - choco install gitversion.portable -pre -y

before_build:
  - nuget restore

build_script:
  - ps: gitversion /l console /proj "C:\projects\phirsoft-topologicalcomparison\phirSOFT.TopologicalComparison.sln" /projargs "/m /verbosity:minimal /logger:'C:\Program Files\AppVeyor\BuildAgent\Appveyor.MSBuildLogger.dll' /p:GeneratePackageOnBuild=false"